stripe.settings:
  path: '/admin/config/stripe'
  defaults:
    _form: '\Drupal\stripe\Form\StripeSettings'
    _title: 'Stripe settings'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE
